// Float型構造体での算術演算テスト

struct PointF {
  x :Float,
  y :Float
}

fn distanceSquared p1 :PointF -> p2 :PointF -> Float {
  let dx = p2.x - p1.x
  let dy = p2.y - p1.y
  dx * dx + dy * dy
}

let p1 = PointF { x: 1.5, y: 2.5 }
let p2 = PointF { x: 4.5, y: 6.5 }
let d = distanceSquared p1 p2
show d

