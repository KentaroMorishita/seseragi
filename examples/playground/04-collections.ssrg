fn evenLabels limit: Int -> Array<String> =
  [`#${value}` | value <- 1..=limit, value % 2 == 0]

// Rangeから選んだ値を文字列へ変換し、joinで区切って表示します。
pub effect fn main =
  evenLabels 8
  |> join " / "
  |> println
