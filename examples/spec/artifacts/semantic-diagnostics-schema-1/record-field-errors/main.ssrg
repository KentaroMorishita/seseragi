fn duplicate -> { name: String } =
  { name: "A", name: "B" }

fn missing user: { name: String } -> String =
  user.age

fn badSpread -> { name: String } =
  { ...42, name: "A" }

fn badPattern user: { name: String } -> String =
  match user {
    { missing } -> missing
  }
