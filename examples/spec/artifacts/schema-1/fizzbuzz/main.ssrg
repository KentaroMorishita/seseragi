fn fizzBuzz number: Int -> String =
  match (number % 3, number % 5) {
    (0, 0) -> "FizzBuzz"
    (0, _) -> "Fizz"
    (_, 0) -> "Buzz"
    _ -> `${number}`
  }

pub fn fizzBuzzValues unit: Unit -> Array<String> =
  [fizzBuzz number | number <- 1..=30]
