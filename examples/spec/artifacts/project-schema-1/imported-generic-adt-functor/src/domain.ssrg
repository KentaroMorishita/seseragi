pub type Box<A> =
  | Boxed A

pub trait Functor<F<_>> {
  fn map<A, B> f: (A -> B) -> value: F<A> -> F<B>
}

instance Functor<Box> {
  fn map<A, B> f: (A -> B) -> value: Box<A> -> Box<B> =
    match value {
      Boxed item -> Boxed $ f item
    }
}

pub fn transform<F<_>, A, B>
  f: (A -> B)
  -> value: F<A>
  -> F<B>
where Functor<F> =
  map f value
