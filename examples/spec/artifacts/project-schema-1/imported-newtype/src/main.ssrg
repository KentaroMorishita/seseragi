import { UserId } from "./domain"

fn raw id: UserId -> Int =
  match id {
    UserId value -> value
  }

fn render id: UserId -> String =
  match raw id {
    42 -> "Imported newtype: 42"
    _ -> "unexpected imported newtype"
  }

pub effect fn main =
  42
  |> UserId
  |> render
  |> println
