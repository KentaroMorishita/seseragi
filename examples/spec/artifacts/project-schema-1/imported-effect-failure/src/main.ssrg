import { InputError, reject } from "./input"

pub type AppError deriving Show =
  | InputFailure InputError

pub effect fn main =
  do {
    mapError InputFailure (reject "lizard")
    succeed ()
  }
