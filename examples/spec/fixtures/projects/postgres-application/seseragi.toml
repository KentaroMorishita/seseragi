[package]
name = "fixture/postgres-application"
version = "0.0.0"
language = ">=0.1.0 <0.2.0"

[run]
entry = "main"
target = "process"

[dependencies]
postgres = { package = "seseragi/postgres", path = "../../../packages/postgres" }
