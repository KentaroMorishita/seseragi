[package]
name = "fixture/module-generic-nominal-identity"
version = "0.0.0"
language = ">=0.1.0 <0.2.0"

[run]
entry = "main"
target = "web"

[dependencies]
ui = { package = "fixture/module-generic-nominal-ui", path = "vendor/ui" }
