[package]
name = "fixture/dts-declaration-merge"
version = "0.0.0"
language = ">=0.1.0 <0.2.0"

[foreign.typescript]
resolver = "node"
manifest = "host/package.json"
lockfile = "host/bun.lock"
bindings = "seseragi.bindings.toml"
