[package]
name = "fixture/package-path-dependency"
version = "0.0.0"
language = ">=0.1.0 <0.2.0"

[run]
entry = "main"
target = "test-js"

[dependencies]
math = { package = "fixture/math", path = "vendor/math" }
