[package]
name = "fixture/test-discovery"
version = "0.0.0"
language = ">=0.1.0 <0.2.0"

[exports]
"." = "library"

[test]
target = "test-js"
jobs = 2
timeout_ms = 30000
cleanup_grace_ms = 5000
seed = 0
