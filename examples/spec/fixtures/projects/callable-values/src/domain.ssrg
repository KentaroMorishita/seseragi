pub fn identity<A> value: A -> A = value
pub let forward = identity
pub fn returned x: Int -> Int -> Int = \y -> x + y
pub fn same<A> x: A -> A -> Bool where Eq<A> = \y -> x == y
pub fn local unit: Unit -> Int = {
  let keep = forward
  let text = keep "ok"
  keep 1
}
