[package]
name = "fixture/benchmark-discovery"
version = "0.0.0"
language = ">=0.1.0"

[exports]
"." = "library"

[benchmark]
target = "node"
warmup = 3
samples = 5
minimum_sample_ms = 10
regression_threshold_percent = 5.0
