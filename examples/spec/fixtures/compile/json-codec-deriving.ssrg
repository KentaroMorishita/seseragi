import { DecodeError, Json } from "std/json"

struct User deriving JsonEncode, JsonDecode {
  id: Int,
  name: String,
}

fn encode user: User -> Json = encodeJson user

fn decode value: Json -> Either<DecodeError, User> = decodeJson value
