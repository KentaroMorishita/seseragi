import * as html from "std/web/html"

type Action =
  | Clicked

fn view -> html.Html<Action> =
  html.div {
    id: "app",
    children: html.button {
      onClick: Clicked,
      children: "click"
    }
  }
