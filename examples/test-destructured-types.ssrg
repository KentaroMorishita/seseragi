// タプル展開後の変数型テスト
let point = (10, 20)
let (x, y) = point

let person = ("Alice", 25, true)
let (name, age, isActive) = person

// ワイルドカード付き
let mixed = (42, "hello", 3.14, false)
let (num, _, pi, flag) = mixed

// 関数からのタプル
fn makeCoordinate a b = (a, b)
let coord = makeCoordinate 100 200
let (coordX, coordY) = coord