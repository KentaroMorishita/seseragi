type Badge =
  | Active

trait Label<A> {
  fn label value: A -> String }

instance Label<Badge> {
  fn label value: Badge -> String = "ACTIVE"
}

fn describe<A> value: A -> String where Label<A> = `Badge: ${label value}`

pub effect fn main = describe Active |> println
