import * as html from "std/web/html"

type Action =
  | NoAction

fn heading -> html.Html<Action> = html.h2 { children: "Child component" }

let page =
  html.main {
    children: [heading (), html.p { children: "Sibling" }]
  }

pub effect fn main = html.renderToString page |> println
