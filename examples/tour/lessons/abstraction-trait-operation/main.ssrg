type Badge =
  | Active

trait Label<A> {
  fn label value: A -> String }

fn labelBadge value: Badge -> String =
  match value {
    Active -> "ACTIVE"
  }

pub effect fn main = labelBadge Active |> println
