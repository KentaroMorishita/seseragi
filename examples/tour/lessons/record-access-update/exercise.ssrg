let profile = { name: "Mio", role: "Engineer" }
let renamed = { ...profile, name: "Ren" }

pub effect fn main =
  do {
    println $ `before: ${profile.name}`
    println $ `after: ${renamed.name}`
  }
