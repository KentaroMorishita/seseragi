import * as html from "std/web/html"

type Action =
  | NoAction

fn planForm title: String -> html.Html<Action> = html.form {
  children: [
    html.h1 { children: title },
    html.input { id: "draft", inputType: "text", value: "" },
    html.button { buttonType: "submit", children: "Save" },
  ]
}

fn page -> html.Html<Action> = html.main { children: planForm "Plan board" }

pub effect fn main = page () |> html.renderToString |> println
