import * as signals from "std/signal"
import { Signal } from "std/signal"

fn observe source: Signal<String> -> Task<String> = signals.read source

pub effect fn main = do {
  state <- signals.make "visible"
  current <- observe state
  println `read-only: ${current}`
}
