fn rank value: Int -> String = `Rank ${value}`

fn render value: Maybe<String> -> String = match value {
  Nothing -> "Nothing"
  Just item -> `Just ${item}`
}

pub effect fn main = do {
  let source: Maybe<Int> = Just 7
  map rank source |> render |> println
  rank <$> source |> render |> println
}
