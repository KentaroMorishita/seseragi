type Delivery =
  | Preparing
  | Shipped String

fn message delivery: Delivery -> String =
  match delivery {
    Preparing -> "Preparing your order"
    Shipped city -> `Shipped to ${city}`
  }

// すべてのconstructorを扱うmatchは網羅的です。
pub effect fn main = Shipped "Osaka" |> message |> println
