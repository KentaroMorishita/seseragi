fn surround left: String -> right: String -> value: String -> String =
  `${left}${value}${right}`

let angle = surround "<" ">"
let paren = surround "(" ")"

pub effect fn main = println `${angle "ready"} ${paren "go"}`
