fn surround left: String -> right: String -> value: String -> String =
  `${left}${value}${right}`

let withLeft = surround "["
let withSides = withLeft "]"
let message = withSides "Seseragi"

pub effect fn main =
  println message
