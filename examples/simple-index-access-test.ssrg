// Simple Array and Tuple Index Access Test

// Array access
let numbers = [1, 2, 3]
let first = numbers[0]

// Tuple access  
let point = (10, 20)
let x = point[0]
let y = point[1]

// Nested access
let matrix = [[1, 2], [3, 4]]
let element = matrix[0][1]

show first
show x  
show y
show element