{
  "buildCommand": "./scripts/build-vercel.sh && cd playground && bun install && bun run build",
  "outputDirectory": "playground/dist",
  "installCommand": "bun install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}