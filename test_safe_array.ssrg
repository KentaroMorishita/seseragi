// 安全な配列アクセスのテスト
let arr = [1, 2, 3]

// 配列の長さを取得
show arr.length

// 安全な配列アクセス（範囲内）
let x = arr[0]
match x {
  Just value -> show value
  Nothing -> show "index out of bounds"
}

// 安全な配列アクセス（範囲外）
let y = arr[10]
match y {
  Just value -> show value
  Nothing -> show "index out of bounds"
}
