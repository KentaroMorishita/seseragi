name: Claude Code Review

on:
  workflow_dispatch:

jobs:
  claude-review:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Claude review workflow is disabled; retained temporarily for cleanup history."
