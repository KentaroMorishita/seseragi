name: VS Code Extension

on:
  pull_request:
    paths:
      - ".github/workflows/vscode-extension.yml"
      - ".github/workflows/linux-native.yml"
      - "scripts/linux-native*"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "crates/seseragi-lsp/**"
      - "crates/seseragi-release/**"
      - "extensions/seseragi/**"
      - "extensions/seseragi-legacy/**"
      - "scripts/check-extension-identity.ts"
      - "scripts/release-contract.ts"
      - "crates/seseragi-driver/**"
      - "crates/seseragi-semantics/**"
      - "crates/seseragi-source/**"
  push:
    branches: [main]
    paths:
      - ".github/workflows/vscode-extension.yml"
      - ".github/workflows/linux-native.yml"
      - "scripts/linux-native*"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "crates/seseragi-lsp/**"
      - "crates/seseragi-release/**"
      - "extensions/seseragi/**"
      - "extensions/seseragi-legacy/**"
      - "scripts/check-extension-identity.ts"
      - "scripts/release-contract.ts"
      - "crates/seseragi-driver/**"
      - "crates/seseragi-semantics/**"
      - "crates/seseragi-source/**"
  workflow_dispatch:
    inputs:
      linux-portability-only:
        description: Run only the downloaded Linux CLI/LSP/VSIX portability gate
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  test:
    if: inputs.linux-portability-only != true
    name: shared extension and LSP tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97.1
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9
      - name: Install extension dependencies
        working-directory: extensions/seseragi
        run: bun install --frozen-lockfile
      - name: Check version and extension identity contracts
        run: |
          bun scripts/release-contract.ts check
          bun scripts/check-extension-identity.ts
      - name: Test extension behavior
        working-directory: extensions/seseragi
        run: bun test tests
      - name: Test native language server
        run: cargo test -p seseragi-lsp

  package:
    if: inputs.linux-portability-only != true
    name: ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            target: darwin-arm64
          - runner: macos-15-intel
            target: darwin-x64
          - runner: windows-latest
            target: win32-x64
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97.1
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9
      - name: Install extension dependencies
        working-directory: extensions/seseragi
        run: bun install --frozen-lockfile
      - name: Package, extract, and smoke test platform VSIX
        working-directory: extensions/seseragi
        env:
          SESERAGI_EXTENSION_TARGET: ${{ matrix.target }}
        run: bun scripts/package-extension.ts
      - uses: actions/upload-artifact@v4
        with:
          name: seseragi-vscode-${{ matrix.target }}
          path: target/seseragi-v*-vscode-${{ matrix.target }}.vsix
          if-no-files-found: error

  migration:
    name: legacy migration stub
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9
      - name: Install extension dependencies
        working-directory: extensions/seseragi
        run: bun install --frozen-lockfile
      - name: Verify migration boundary and package the legacy stub
        working-directory: extensions/seseragi
        run: bun run package:legacy
      - uses: actions/upload-artifact@v4
        with:
          name: seseragi-vscode-legacy-migration
          path: target/seseragi-legacy-migration-v*.vsix
          if-no-files-found: error

  linux-native:
    uses: ./.github/workflows/linux-native.yml
    with:
      source-ref: ${{ github.sha }}
