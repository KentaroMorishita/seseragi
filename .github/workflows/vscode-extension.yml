name: VS Code Extension

on:
  pull_request:
    paths:
      - ".github/workflows/vscode-extension.yml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "crates/seseragi-lsp/**"
      - "crates/seseragi-release/**"
      - "extensions/seseragi/**"
      - "extensions/seseragi-legacy/**"
      - "scripts/check-extension-identity.ts"
      - "scripts/release-contract.ts"
      - "crates/seseragi-driver/**"
      - "crates/seseragi-semantics/**"
      - "crates/seseragi-source/**"
  push:
    branches: [main]
    paths:
      - ".github/workflows/vscode-extension.yml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - "crates/seseragi-lsp/**"
      - "crates/seseragi-release/**"
      - "extensions/seseragi/**"
      - "extensions/seseragi-legacy/**"
      - "scripts/check-extension-identity.ts"
      - "scripts/release-contract.ts"
      - "crates/seseragi-driver/**"
      - "crates/seseragi-semantics/**"
      - "crates/seseragi-source/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: shared extension and LSP tests
    # Promotion's exact full gate already covers extension behavior and the LSP.
    # Release builds verified platform artifacts after merge, so do not duplicate
    # this lane on immutable promote/* PRs.
    if: github.event_name != 'pull_request' || !startsWith(github.head_ref, 'promote/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97.1
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9
      - name: Install extension dependencies
        working-directory: extensions/seseragi
        run: bun install --frozen-lockfile
      - name: Check version and extension identity contracts
        run: |
          bun scripts/release-contract.ts check
          bun scripts/check-extension-identity.ts
      - name: Test extension behavior
        working-directory: extensions/seseragi
        run: bun test tests
      - name: Test native language server
        run: cargo test -p seseragi-lsp

  package:
    name: ${{ matrix.target }}
    # The canonical release workflow packages/smoke-tests the same four targets
    # before publish. Promotion PRs should not run that matrix twice.
    if: github.event_name != 'pull_request' || !startsWith(github.head_ref, 'promote/')
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            target: darwin-arm64
          - runner: macos-15-intel
            target: darwin-x64
          - runner: ubuntu-latest
            target: linux-x64
          - runner: windows-latest
            target: win32-x64
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97.1
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9
      - name: Install extension dependencies
        working-directory: extensions/seseragi
        run: bun install --frozen-lockfile
      - name: Package, extract, and smoke test platform VSIX
        working-directory: extensions/seseragi
        env:
          SESERAGI_EXTENSION_TARGET: ${{ matrix.target }}
        run: bun scripts/package-extension.ts
      - uses: actions/upload-artifact@v4
        with:
          name: seseragi-vscode-${{ matrix.target }}
          path: target/seseragi-v*-vscode-${{ matrix.target }}.vsix
          if-no-files-found: error

  migration:
    name: legacy migration stub
    if: github.event_name != 'pull_request' || !startsWith(github.head_ref, 'promote/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9
      - name: Install extension dependencies
        working-directory: extensions/seseragi
        run: bun install --frozen-lockfile
      - name: Verify migration boundary and package the legacy stub
        working-directory: extensions/seseragi
        run: bun run package:legacy
      - uses: actions/upload-artifact@v4
        with:
          name: seseragi-vscode-legacy-migration
          path: target/seseragi-legacy-migration-v*.vsix
          if-no-files-found: error
